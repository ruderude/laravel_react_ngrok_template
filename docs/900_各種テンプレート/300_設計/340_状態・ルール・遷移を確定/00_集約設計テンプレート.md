# 集約：【集約名】

## 概要

- **責務**:
- **識別子**:
- **含まれるもの**:

---

## 状態

```
[初期] ─→ 状態A ─→ 状態B ─→ 状態C
                ↑_________↓（差し戻し可）
```

| 状態 | enum値 | 説明 |
|----|-------|----|
|    |       |    |

---

## コマンド

### コマンド名（英語名）

- **許可**: 状態A, 状態B
- **遷移**: → 状態C
- **引数**: arg1, arg2?
- **失敗**: ExceptionName

<!--
例：
### 担当者を割り当てる（assignTo）

- **許可**: 未対応, 対応中
- **遷移**: → 対応中
- **引数**: assigneeId, comment?
- **失敗**: InvalidStatusException, EmployeeNotFoundException
-->

---

## 不変条件

- [ ] 条件1（`if (xxx) throw`）
- [ ] 条件2

<!--
例：
- [ ] 完了状態では担当者変更不可（`if (status === COMPLETED) throw`）
- [ ] 未対応→完了の直接遷移は不可
- [ ] 対応履歴は削除できない
-->

---

## 値オブジェクト

| 名前 | 型 | 検証 | 正規化 |
|----|---|----|-----|
|    |   |    |     |

<!--
例：
| 名前 | 型 | 検証 | 正規化 |
|------|------|------|--------|
| ReportCategory | enum | 定義値のみ | - |
| Comment | string | 1〜1000文字 | trim |
| Email | string | RFC準拠 | 小文字化, trim |
-->

---

## 外部参照

- **参照先**: IDのみ保持（例: EmployeeId）
- **禁止**: 他集約のオブジェクト直接参照

---

## ドメイン例外

<!-- この集約で throw する例外を列挙する。すべて DomainException を継承する -->

| 例外クラス名 | 発生条件 | 例外ハンドラでの HTTP ステータス |
|--------|------|---------------------|
|        |      |                     |

<!--
例：
| 例外クラス名 | 発生条件 | 例外ハンドラでの HTTP ステータス |
|----------|--------|---------------------------|
| FriendNotFoundException | 指定 FID の友だちが存在しない | 404 |
| DuplicateLineUserIdException | LINE ユーザーID が重複 | 409 |
| InvalidStatusTransitionException | 不正なステータス遷移 | 409 |
-->

---

## 禁止事項

-

<!--
例：
- 対応履歴の削除・編集
- setter による直接状態変更
- 集約内での他リポジトリ呼び出し
- 集約内での外部 API 呼び出し
- Eloquent や Laravel への依存
-->
